import { PagePlaceholder } from "@/components/page-placeholder";

export function AuditPage() {
  return (
    <PagePlaceholder
      description="Les événements d’audit seront consultables depuis cette page."
      title="Audit"
    />
  );
}
